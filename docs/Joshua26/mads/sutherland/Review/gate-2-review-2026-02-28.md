- **Gate 2: FAILED** — Implementation diverges from the approved REQ (Ray iteration) and will not run as specified (Ray Serve vs ollama/vLLM mismatch, missing ray-head, broken alias registry wiring). Multiple hard runtime failures remain (DB name mismatch, missing alias rows, invalid backend URLs), and documentation/tests are out of compliance.

- **Blockers:**
  - **REQ (Ray iteration) vs implementation mismatch (Ray Serve vs ollama/vLLM, missing ray-head):** REQ defines a Ray Serve cluster with `sutherland-ray-head` and `local_ray` routing, but `docker-compose.yml` deploys `ollama/ollama` + `vllm/vllm-openai` containers and does not define `sutherland-ray-head`. `sutherland-langgraph` and gateway config still assume Ray (`backend_type=local_ray`, `RAY_HEAD_URL`, dependency on `sutherland-ray-head`). This will fail at runtime and violates the delta REQ. Files: `mads/sutherland/docs/REQ-sutherland.md`, `docker-compose.yml`, `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/sutherland/config.json`.
  - **Postgres database name mismatch (runtime failure):** Compose sets `POSTGRES_DB=sutherlanddb`, while `sutherland-langgraph` and gateway dependency config use `sutherland` as the database name. Alias lookups will fail because the expected database doesn’t exist. Files: `docker-compose.yml`, `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/sutherland/config.json`, `mads/sutherland/sutherland-postgres/init.sql`.
  - **Alias seed mismatch for embeddings/rerank (runtime 500s):** `sutherland-langgraph` hard-codes aliases `embed-default` and `bge-reranker-v2-m3`, but `init.sql` seeds `primary-embedder` and `primary-reranker`. Embedding and rerank routes will return 500 (“alias missing”). Files: `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/sutherland-postgres/init.sql`.
  - **Backend URL missing Ray Serve endpoints (runtime failure):** `init.sql` sets `backend_url` to `http://sutherland-ray-head:8000` without a deployment path, but `server.py` POSTs directly to `backend_url` for embeddings/rerank. REQ specifies deployment endpoints like `/embedding` and `/reranker`. This will 404 or hit the wrong route. Files: `mads/sutherland/sutherland-postgres/init.sql`, `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/docs/REQ-sutherland.md`.
  - **OpenAI-compatible message handling is incomplete (breaks multi-turn + vision):** `llm_chat_completions` only converts `system` and `user` roles into LangChain messages and silently drops `assistant`/tool roles. It also does not map OpenAI multimodal content blocks into LangChain message formats. This violates the REQ’s OpenAI-compatible messages requirement and will break multi-turn context/vision calls. Files: `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/docs/REQ-sutherland.md`.
  - **Unit tests missing for new programmatic logic:** REQ-000 §1.10 mandates unit tests for new programmatic logic in the langgraph service. Only integration tests exist (`mads/sutherland/tests/test_sutherland.py`), and there are no pytest unit tests for the alias resolution/routing logic. Files: `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/tests/test_sutherland.py`.
  - **Unpinned inference image tags (REQ-000 §1.4/§1.7):** `ollama/ollama` and `vllm/vllm-openai:latest` are unpinned, violating version pinning and offline build requirements. Files: `docker-compose.yml`.
  - **README is stale and contradicts REQ (Ray iteration):** `mads/sutherland/README.md` still documents GPUStack, legacy tools, and port 80 UI, none of which exist in the REQ (Ray iteration) or current containers. REQ-000 §6.2 requires accurate architecture and tool documentation. Files: `mads/sutherland/README.md`, `mads/sutherland/docs/REQ-sutherland.md`.

- **Observations:**
  - **Embedding/rerank routes lack direct payload validation:** `server.py` assumes `payload["input"]` or `payload["documents"]` exists. Gateway schema validation should cover this, but direct HTTP calls to langgraph will 500 instead of a structured 4xx response. File: `mads/sutherland/sutherland-langgraph/server.py`.
  - **Timeouts differ from REQ values:** REQ recommends 60s for embeddings and 30s for rerank; current code uses 30s for embeddings and 30s for rerank. If intentional, document in REQ or config. Files: `mads/sutherland/sutherland-langgraph/server.py`, `mads/sutherland/docs/REQ-sutherland.md`.
  - **Test suite not updated for new tool set:** `test_sutherland.py` does not exercise `llm_embeddings` and still describes “State 1” and ollama-based routing. File: `mads/sutherland/tests/test_sutherland.py`.

- **Recommendations:**
  - **Align REQ and implementation immediately:** Either (a) implement the Ray Serve architecture and update compose/code accordingly (add `sutherland-ray-head`, fix alias endpoints, remove ollama/vllm), or (b) revert the REQ to the previous (llamacpp/vllm) topology and update it accordingly. Do not proceed to deployment with split architecture.
  - **Add focused unit tests for alias resolution and routing:** Unit tests should cover alias missing, backend_type branches (local_ray/openai/gemini), and response shaping for embeddings/rerank. Keep integration tests for end-to-end validation.
  - **Update README and tests to the current tool surface:** Ensure docs reflect the three tools (`llm_chat_completions`, `llm_embeddings`, `llm_rerank`), Ray Serve topology, and correct ports/health endpoints.
